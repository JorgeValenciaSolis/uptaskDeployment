<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        //-Instalamos jucie en node para que se le pase estilos al motor de email
        style
            include ./email.css

    body
        h2 Confirma tu cuenta UpTask
        p Ya estas casi inscrito, solo presiona el siguiete boton
        a(href=`${confirmarUrl}`) Confirmar Cuenta
        p Si no puedes acceder a este enlace, visita #{confirmarUrl}